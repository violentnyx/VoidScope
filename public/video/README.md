# Fallback de vídeo do tema

Coloque aqui o vídeo usado quando o ShaderLab/WebGPU não estiver disponível:

- `shader-background-fallback.mp4`

O vídeo deve ser curto, sem áudio, em loop e otimizado para web. Se ele estiver ausente ou falhar, o site usa automaticamente o fallback CSS.
